// Memory ABI declarations; the freestanding backend binds these names later.
fn alloc(size: int) -> int { return 0 }
fn alloc_array(size: int) -> int { return 0 }
fn copy(dst: int, src: int, size: int) {}
fn set(dst: int, value: int, size: int) {}
